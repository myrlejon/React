const homeView = "/"
const aboutView = "/about"
const signInView = "/signin"
const profileView = "/profile"
const settingsView = "/settings"

export default {
    homeView,
    aboutView,
    signInView,
    profileView,
    settingsView
}